# 安全说明

本项目涉及跨境电商销售数据、商家资源和大模型 API 密钥。使用和同步 GitHub 时请遵守：

- 不提交 `用户数据/`。
- 不提交 `卖家精灵数据表/`。
- 不提交 `deepseek.env`、`deepseek.env.rtf`、`.env` 或任何 API Key。
- 不提交含商家电话、邮箱、地址的文件。
- 如误提交密钥，应立即在服务商后台作废该密钥并重新生成。

仓库中的 `.env.example` 只是格式示例，不包含真实密钥。
